import { ColourCodePipe } from './colour-code.pipe';

describe('ColourCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ColourCodePipe();
    expect(pipe).toBeTruthy();
  });
});
